class Friendship {
    constructor(id, ownerUser, friendUser, friendNickname, friendBackgroundColor, friendForegroundColor) {
        this.id = id
        this.ownerUser = ownerUser
        this.friendUser = friendUser
        this.friendNickname = friendNickname
        this.friendBackgroundColor = friendBackgroundColor
        this.friendForegroundColor = friendForegroundColor
    }
}

export default Friendship